"use client";
import styles from "./pathbot.module.css";

export default function PathBotPage() {
    return (
        <main className={styles.container}>
        <h2 className={styles.title}>Asistente virtual en desarrollo</h2>
        </main>
    );
}
