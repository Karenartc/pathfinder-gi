"use client";

export default function AdminPage() {
    return (
        <main
        style={{
            display: "flex",
            justifyContent: "center",
            alignItems: "center",
            height: "80vh",
            fontSize: "1.5rem",
            fontWeight: 600,
            color: "rgb(var(--color-primary))",
        }}
        >
        Admin
        </main>
    );
}
